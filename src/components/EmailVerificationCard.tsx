/**
 * Email verification was removed. This component now renders nothing
 * to preserve any existing imports without breaking layouts.
 */
export const EmailVerificationCard = (_: { compact?: boolean }) => null;
export default EmailVerificationCard;
